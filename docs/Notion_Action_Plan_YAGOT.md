# 🎯 خطة التنفيذ المعتمدة لمشروع ياقوت (YAGOT) — لوحة تتبع Notion

> 📌 **استراتيجية التنفيذ:** معالجة الأساس أولاً (الأمان والبنية التحتية) قبل الانتقال إلى تحسينات الأكواد والواجهة لضمان استقرار وسرعة المتجر دون أي توقف أو كسر في الخدمات.

---

## 📊 جدول تتبع المراحل (Notion Tracking Table)

| # | المرحلة (Phase) | الأولوية | المكون الرئيسي | الحالة (Status) | تاريخ الإنجاز |
|---|---|---|---|---|---|
| **1** | **المرحلة 1 — Security** | 🔴 P0 (حرجة) | الأمان والأسرار | ⏳ مؤجلة مؤقتاً | - |
| **2** | **المرحلة 2 — Database Region** | 🟠 P1 (عالية) | البنية التحتية والشبكة | 🟢 **تم الإنجاز** | 2026-09-08 |
| **3** | **المرحلة 3 — Persistent Storage** | 🔴 P0 / P1 | التخزين السحابي للوسائط | 🟢 **تم الإنجاز** | 2026-09-08 |
| **4** | **المرحلة 4 — Caching** | 🟠 P1 (عالية) | كاش Kestrel & Cloudflare | 🔄 **جاري التنفيذ** | - |
| **5** | **المرحلة 5 — Images** | 🟠 P1 (عالية) | تحسين وضغط الصور | ⏳ قيد الانتظار | - |
| **6** | **المرحلة 6 — Backend** | 🟡 P2 (متوسطة) | منطق الخادم والاستعلامات | ⏳ قيد الانتظار | - |
| **7** | **المرحلة 7 — Database** | 🟡 P2 (متوسطة) | فهارس PostgreSQL | ⏳ قيد الانتظار | - |
| **8** | **المرحلة 8 — Frontend** | 🟢 P3 (منخفضة) | تحسين أصول الواجهة | ⏳ قيد الانتظار | - |

---

## 📝 تفاصيل المهام التنفيذية لكل مرحلة

### 🔴 المرحلة 1 — Security (الأمان وتطهير الأسرار)
- [ ] **1.1 تدوير كافة كلمات المرور والمفاتيح المكشوفة:**
  - [ ] تغيير كلمة مرور قاعدة بيانات Neon الأساسية (`MYDB`).
  - [ ] تغيير كلمة مرور قاعدة بيانات المستخدمين (`UsersConnection`).
  - [ ] إعادة إنشاء `ClientSecret` لـ Google OAuth من Google Cloud Console.
  - [ ] تغيير كلمة مرور تطبيق Gmail SMTP.
  - [ ] توليد مفتاح تشفير AES جديد (`Key` 32 بايت و `IV` 16 بايت).
- [ ] **1.2 نقل البيانات السرية إلى Environment Variables:**
  - [ ] حقن سلاسل الاتصال والمفاتيح في Render Environment Secrets.
  - [ ] تنظيف `appsettings.json` و `appsettings.Development.json` واستبدال القيم بنماذج فارغة.
  - [ ] إضافة `appsettings.Development.json` لملف `.gitignore`.
- [ ] **1.3 إزالة سلاسل الاتصال الثابتة من الكود:**
  - [ ] حذف سلسلة الاتصال الصريحة من `Models/NeondbContext.cs` (`OnConfiguring`).
  - [ ] حذف سلسلة الاتصال الصريحة من `Data/UsersDbContext.cs` (`OnConfiguring`).
- [ ] **1.4 تفعيل حماية كوكي Antiforgery:**
  - [ ] ضبط `CookieSecurePolicy.Always` في `Program.cs`.

---

### 🟠 المرحلة 2 — Database Region (معالجة الموقع الجغرافي لقاعدة البيانات)
- [ ] **2.1 نقل أو إعادة إنشاء فرع قاعدة بيانات `MYDB`:**
  - [ ] نقل قاعدة البيانات من Frankfurt (`eu-central-1`) إلى Oregon (`us-west-2`) لتتواجد بجوار خوادم Render (`us-west-1`).
  - [ ] تحديث متغير الاتصال `ConnectionStrings__MYDB` في خادم Render.
- [ ] **2.2 التحقق من انخفاض التأخير:**
  - [ ] فحص زمن الاستجابة (Ping) والتأكد من انخفاضه من ~150ms إلى <2ms لكل استعلام.
  - [ ] التحقق من انخفاض زمن استجابة الصفحة الأولى TTFB من >1s إلى <300ms.

---

### 🔴 المرحلة 3 — Persistent Storage (التخزين الدائم لصور المنتجات والإيصالات)
- [ ] **3.1 ربط التخزين السحابي الخارجي (S3 / Cloudflare R2 / Uploadcare):**
  - [ ] تهيئة حساب وحاوية تخزين سحابية Bucket خارجية غير قابلة للضياع.
- [ ] **3.2 تعديل خدمة رفع الصور `Services/Image.cs`:**
  - [ ] توجيه عمليات حفظ وتعديل صور المنتجات إلى الـ Bucket السحابي بدلاً من القرص المحلي `wwwroot/images/products`.
- [ ] **3.3 تعديل خدمة إيصالات الدفع `Services/ReceiptStorageService.cs`:**
  - [ ] حفظ إيصالات التحويل البنكي المشفرة في مسار سحابي خاص وآمن بدلاً من مجلد الحاوية المؤقت `App_Data`.
- [ ] **3.4 نقل الصور الحالية:**
  - [ ] رفع الأصول الحالية إلى التخزين الدائم لإنهاء مشكلة أخطاء 404 نهائياً.

---

### 🟠 المرحلة 4 — Caching (إصلاح ترويسات الكاش و Cloudflare CDN)
- [ ] **4.1 تعديل إعدادات `app.UseStaticFiles` في `Program.cs`:**
  - [ ] إزالة استثناء عدم تطبيق الكاش على غير الخطوط.
  - [ ] تطبيق `Cache-Control: public, max-age=31536000, immutable` على كافة ملفات CSS/JS/Images التي تحمل إصداراً (`v=...`).
  - [ ] تطبيق `Cache-Control: public, max-age=86400` على الأصول العامة الأخرى.
- [ ] **4.2 التحقق الميداني مع Cloudflare:**
  - [ ] فحص الترويسات عبر المتصفح والتأكد من تحول `cf-cache-status` من `DYNAMIC` إلى `HIT`.

---

### 🟠 المرحلة 5 — Images (ضغط الوسائط وصورة الـ Hero)
- [ ] **5.1 ضغط صورة الهيرو الرئيسية:**
  - [ ] تحويل `perfume-hero-golden-v1.png` (2.3 MB) إلى صيغة WebP بأبعاد متجاوبة وحجم <100 KB.
- [ ] **5.2 إضافة الصورة الناقصة البديلة:**
  - [ ] توفير أيقونة `6389130_camera_interface_movie_picture_zoom_icon.png` لحل خطأ 404 للأيقونة الافتراضية.
- [ ] **5.3 معالجة وتحويل صور المنتجات:**
  - [ ] ضغط ملفات `.jfif` و `.jpeg` الكبيرة وتحويلها تدريجياً لصيغ WebP خفيفة مع خاصية `loading="lazy"`.

---

### 🟡 المرحلة 6 — Backend (معالجة الاستعلامات المتسلسلة والكاش)
- [ ] **6.1 تحسين استعلامات الصفحة الرئيسية (`HomeController.cs`):**
  - [ ] تفعيل `IMemoryCache` لحفظ بيانات الأقسام، الماركات، والأكثر مبيعاً لمدد محددة (5-10 دقائق).
  - [ ] إلغاء الاستعلامات المتتالية وتخفيف الحمل المتكرر على قاعدة البيانات.
- [ ] **6.2 عزل خدمة الترخيص وفحص الإغلاق (`DealingAPI.cs`):**
  - [ ] منع قفل المتجر (`StatueSite.Developer`) عند انقطاع الاتصال مع `controlpanel.logicoreplus.com` وتحويل السلوك إلى Fail-Open مع تسجيل تنبيه.
- [ ] **6.3 تحسين ملخص المخزون في لوحة الإدارة (`ProductsController.cs`):**
  - [ ] كاش نتائج ملخص المخزون `inventorySummary` لتفادي Full Table Scan في كل زيارة.

---

### 🟡 المرحلة 7 — Database (فهارس قاعدة البيانات والاستعلامات)
- [ ] **7.1 إضافة فهارس استعلامات البحث والفلترة عبر Migration جديد:**
  - [ ] إضافة فهرس btree على عمود السعر: `products (price)`.
  - [ ] إضافة فهرس وظيفي على الماركات: `CREATE INDEX ix_products_brand_lower ON products (LOWER(brand));`.
  - [ ] إضافة فهرس GIN Trigram على أسماء المستخدمين في قاعدة بيانات Users: `CREATE INDEX ix_users_name_trgm ON users USING gin (name gin_trgm_ops);`.

---

### 🟢 المرحلة 8 — Frontend (التصغير والتحزيم والتحميل الكسول)
- [ ] **8.1 تصغير ملفات الجافاسكريبت والـ CSS (Minification/Bundling):**
  - [ ] تصغير `cart-drawer.js` (53 KB) ودمجه مع الحزم الأساسية أو تحميله فقط عند الحاجة.
  - [ ] تصغير ملفات `yaqut-theme.css` و `layout.css`.
- [ ] **8.2 تنظيف وتخفيف الخطوط (Web Fonts):**
  - [ ] تقليل عدد ملفات الخطوط المطلوبة دفعة واحدة في `font-delivery.css` وحصرها في الأوزان الفعلية المستخدمة.
- [ ] **8.3 ضبط الـ Lazy Loading للصور أسفل الصفحة.**
